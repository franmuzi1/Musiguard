# MusiGuard: controllo giornaliero dello spazio disco.
# Persistent=true recupera l'esecuzione persa se il PC era spento.

[Unit]
Description=MusiGuard - controlla ogni giorno lo spazio disco

[Timer]
OnCalendar=daily
Persistent=true
RandomizedDelaySec=10min

[Install]
WantedBy=timers.target
